<?php
/**
 * Образец конфигурации прокси.
 *
 * Скопируйте этот файл как skin-test-config.php и положите ВЫШЕ корня сайта —
 * туда, куда веб-сервер не отдаёт файлы напрямую. Например, если корень сайта
 * /var/www/mesoforia/public_html, то положите в /var/www/mesoforia/.
 *
 * Запасной вариант (если выше корня положить некуда): назвать config.php и
 * положить рядом с proxy.php — тогда обязательно закройте его через .htaccess,
 * см. server/.htaccess.
 *
 * Файл не должен попадать в git.
 */

return [
    // Новый ключ из https://console.anthropic.com — старый, засвеченный в
    // прототипе, нужно отозвать.
    'api_key' => 'sk-ant-...',

    // Домены, которым разрешено обращаться к прокси.
    'allowed_origins' => [
        'https://mesoforia.ru',
        'https://www.mesoforia.ru',
    ],

    // Модель и лимит ответа задаются на сервере, клиент их не выбирает.
    'model'      => 'claude-haiku-4-5-20251001',
    'max_tokens' => 1024,

    // Защита от накрутки: сколько запросов с одного IP в час.
    // 0 — лимит выключен.
    'rate_limit_per_hour' => 60,

    // Где хранить счётчики. Каталог должен быть доступен PHP на запись.
    'rate_limit_dir' => sys_get_temp_dir() . '/mesoforia-skin-test',

    // Потолок размера запроса: фото уходит в base64 и весит примерно
    // в 1.37 раза больше исходного файла.
    'max_body_bytes' => 8 * 1024 * 1024,
];
