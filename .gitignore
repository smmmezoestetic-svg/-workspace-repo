# Dependencies
node_modules/
.npm

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
ENV/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Build
# dist/ намеренно НЕ игнорируется: собранные файлы лежат в репозитории,
# чтобы их можно было залить на хостинг без установки Node.
# Исключение — dist/vendor/: это побайтовая копия public/vendor/ весом 11 МБ,
# её создаёт `npm run build`, держать её в истории дважды незачем.
dist/vendor/
build/
*.egg-info/

# Конфиг прокси — содержит ключ Anthropic, в репозиторий не попадает
server/config.php
skin-test-config.php

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment variables
.env
.env.local
.env.*.local

# OS
.DS_Store
Thumbs.db
